"use client";
import { Hero } from "@/features/ui/hero";

export const ReportingHero = () => {
  return (
    <Hero
      title="Chat reporting"
      description="Conversation history across all users. Admin only."
    ></Hero>
  );
};
